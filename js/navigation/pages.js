/** Common pages * */

const commonPages = {
  INITIAL: 'initial',
  ENTRY: 'welcome',
  ERROR: 'error',
  SCHEDULE: 'schedule',
};

const pages = Object.assign({}, commonPages);

export default pages;
