/** Common pages * */

const commonPages = {
  INITIAL: 'initial',
  ENTRY: 'welcome',
  ERROR: 'error',
  SCHEDULE: 'schedule',
  CONTACTS: 'contacts',
};

const pages = Object.assign({}, commonPages);

export default pages;
