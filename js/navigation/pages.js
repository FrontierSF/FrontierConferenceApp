/** Common pages * */

const commonPages = {
  INITIAL: 'initial',
  ENTRY: 'welcome',
  ERROR: 'error',
  SCHEDULE: 'schedule',
  DETAILS: 'details',
  CONTACTS: 'contacts',
};

const pages = Object.assign({}, commonPages);

export default pages;
